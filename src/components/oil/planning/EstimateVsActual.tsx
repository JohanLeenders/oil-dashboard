/**
 * EstimateVsActual — Placeholder voor schatting vs. werkelijk vergelijking
 * Sprint: Wave 3 — A1-S3 EstimateVsActual placeholder
 *
 * REGRESSIE-CHECK:
 * - Server Component (no 'use client')
 * - Placeholder only, no data fetching
 * - Will be replaced with full implementation in a future phase
 */

export default function EstimateVsActual() {
  return (
    <div className="bg-gray-50 dark:bg-gray-900 rounded-lg border border-dashed border-gray-300 dark:border-gray-700 p-6">
      <div className="text-center">
        <h3 className="text-sm font-semibold text-gray-900 dark:text-white">
          Schatting vs. werkelijk
        </h3>
        <p className="mt-1 text-sm text-gray-500 dark:text-gray-400">
          Schatting vs. werkelijk — Wordt toegevoegd in een volgende fase
        </p>
      </div>
    </div>
  );
}
