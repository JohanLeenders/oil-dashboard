import type { NextConfig } from 'next';

/**
 * Next.js Configuration
 *
 * Note: typedRoutes disabled for MVP phase.
 * Reason: Adds complexity without clear benefit at this stage.
 * Can be re-enabled in Phase 3 when route structure stabilizes.
 */
const nextConfig: NextConfig = {
  // typedRoutes disabled - see comment above
};

export default nextConfig;
